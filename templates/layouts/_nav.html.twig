<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container-fluid">
		<a href="{{ path('app_home') }}" class="navbar-brand">Red Bull Racing</a>

		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a href="{{ path('app_home') }}" class="nav-link" >Accueil</a>
				</li>

				<li class="nav-item">
					<a href="{{ path('app_product_index') }}" class="nav-link" >Boutique</a>
				</li>

				{% if is_granted('ROLE_USER') %}
					<li class="nav-item">
						<a href="{{ path('app_basket') }}" class="nav-link" >Panier</a>
					</li>
				{% endif %}

				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item dropdown">
						<a href="{{ path('app_dashboard_product') }}" class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Tableau de bord
						</a>

						<ul class="dropdown-menu" aria-labelledby="navbarDropdown">
							<li><a href="{{ path('app_dashboard_product') }}" class="dropdown-item">Produits</a></li>
							<li><a href="{{ path('app_dashboard_category') }}" class="dropdown-item">Catégories</a></li>
						</ul>
					</li>
				{% endif %}

				{% if is_granted('ROLE_USER') %}
					<li class="nav-item">
						<a href="{{ path('app_logout') }}" class="nav-link">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
					</li>

					<li class="nav-item">
						<a href="{{ path('app_register') }}" class="nav-link">Créer un compte</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>